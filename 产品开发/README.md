# 产品开发

这里存放 Nomos 的开发过程资产。

- `nomos-desktop`：当前桌面客户端源码。
- `测试用例`：产品级、验收级或手工测试用例。
- `测试报告`：版本验收、回归测试、缺陷复盘等报告。
- `中间数据`：开发过程中的临时数据、实验数据和非正式生成物。

开发源码内部仍保留自己的 `tests` 目录，用于自动化测试；产品级测试材料放在本目录的测试用例和测试报告中。
