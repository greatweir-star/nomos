# Nomos V0.0.3 M6 联合验收记录

> 日期：2026-06-21  
> 范围：控制台、迁移、备份恢复、安全、诊断、桌面打包  
> 结论：功能与静态发布门禁通过；进入候选发布制品验证

## 完成内容

- 控制台全部使用真实数据库统计和异常项，不展示固定告警或虚构负载。
- 增加 SQLite 备份、完整性检查、显式确认恢复、恢复前保护备份和失败回滚。
- 增加旧 v9 数据 dry-run、敏感字段报告、确认迁移和来源哈希幂等控制；源文件不被修改。
- 会话密钥仅在内存物化，数据库不保存原值；增加敏感信息扫描、审计查询和安全响应头。
- 应用异常中断后，将执行、派发、容量租约和工作项恢复为可解释状态。
- 新版 Renderer 成为桌面端默认入口，旧版保留为显式兼容入口。
- 建立 macOS Apple Silicon 发布构建、临时签名和自动发布检查；Windows 构建验证已完成但不纳入本版对外发布范围。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 类型检查、单元/集成测试、生产构建 | 通过，92/92 |
| 静态发布门禁 | 通过，Renderer 资源 11 个 |
| 生产依赖漏洞审计 | 0 个已知漏洞 |
| 仓库高风险凭据模式扫描 | 0 个命中 |
| 备份、恢复保护、迁移源文件不变 | 通过 |
| macOS 安装包构建与真实启动 | 通过 |
| 发布平台范围 | macOS Apple Silicon；Windows 暂缓 |
| GitHub macOS 干净环境 CI | 通过，Run 27925383379；测试、构建、打包、签名结构、制品上传全部成功 |

## 发布前外部条件

- macOS 公网分发需 Apple Developer ID 签名及公证；当前候选包未签名。
- Alice、Kimi 未安装在当前验收机，仅通过统一合同自动化测试，连接实机状态标为未验证。
