# Nomos V0.0.3 M4 联合验收记录

> 日期：2026-06-21  
> 范围：FE/BE-301~303、FE-401  
> 结论：通过；流程与项目主链路可用，可进入 Sprint 5

## 完成内容

- FlowDefinition 与 FlowVersion 草稿、图校验和不可变发布。
- 校验器覆盖缺岗位槽位、缺验收标准、断链、无效边和有向环。
- 用户主动导入“软件需求到交付”黄金流程，包含需求澄清、产品设计、技术方案、开发实现、代码评审、测试验证和发布验收七个阶段。
- 项目创建仅接受可调度 Owner 和已发布流程版本。
- 项目启动事务性生成 FlowInstance、七个 NodeInstance 和按岗位槽位拆分的七个 WorkItem。
- 下游工作项记录上游依赖；只有首节点工作项进入 `pending_dispatch`。
- 项目运行页展示冻结版本、节点数、工作项数、依赖与真实状态。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| TypeScript / React 类型检查 | 通过 |
| 生产构建 | 通过 |
| 自动化测试 | 86/86 通过 |
| 环路发布门禁 | 通过，返回 `FLOW_VALIDATION_FAILED` |
| 浏览器黄金流程发布 | 通过，7 节点 / 6 依赖 |
| 浏览器项目启动 | 通过，7 节点实例 / 7 工作项 |
| 首节点与下游依赖状态 | 通过，首项待派发、下游草稿 |
| 页面空白/框架错误覆盖层 | 无 |

## 已知环境事项

- 浏览器截图接口仍在当前 Codex Browser 环境中超时；本轮以页面身份、DOM 快照、真实 API/SQLite 数据和交互后状态作为证据。
- Browser 日志查询会返回先前构建切换时产生的旧资源哈希错误；当前新页面未产生新的应用错误，生产构建资源均正常加载。
